# GenAI Project

Placeholder name — see `project.config.json` (added during monorepo migration) for the single source of truth on branding, so it can change without touching code.

Monorepo for the API backend, web app, and mobile app. Structure and migration in progress.
